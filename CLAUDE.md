# Cosy Frontend

Cosy ("Cost Optimized Server Yard") is a **community-driven, open-source platform for
hosting game servers on your own hardware**. There is no central Cosy deployment:
every user runs their own instance (via Docker Compose or Kubernetes) and Cosy manages
game servers as Docker containers on that machine — container lifecycle, real-time
metrics/logs, file browsing, user & quota management, RCON, and webhooks.

Cosy is a multi-repo project under [github.com/Magenta-Mause](https://github.com/Magenta-Mause):

| Repo | Role |
|---|---|
| **Cosy-Frontend** (this repo) | React 19 + TypeScript web UI on Bun + Vite (TanStack Router/Query, Redux Toolkit, Tailwind v4, shadcn/ui) |
| Cosy-Backend | Core orchestration engine (Spring Boot / Java 21); ships the OpenAPI spec this repo consumes |
| Cosy-Game-Service | Rust (actix-web) game metadata/artwork API (SteamGridDB) |
| Cosy-Template-Service | Go (Gin) service serving the template catalog |
| Cosy-Templates | Curated game/template definitions (schema-validated) |
| Cosy / Cosy-Docs / Cosy-Internal-Deployment | Meta repo + install scripts / docs site / maintainers' reference k8s deployment |

## Working in this repo

- Bun, not npm/node: `bun install`, `bun dev` (Vite on http://localhost:5173),
  `bun typecheck`, `bun lint` (Biome + tsc).
- **The API client is generated.** `src/api/generated/` comes from the backend OpenAPI
  spec via Orval — never edit it by hand. Regenerate with `bun gen:api` while the
  backend runs on localhost:8080. The output is committed so CI never needs a live backend.
- `src/routeTree.gen.ts` is auto-generated by the TanStack Router plugin (regenerated
  by the dev server) — never edit it manually.
- Local dev expects the backend on http://localhost:8080 (dev login `admin` / `admin`).

## Deep dives — read when your task touches the area

- Writing or reviewing **React / TypeScript** code (component structure, state,
  loading/buttons, forms, styling, icons, routing, i18n, testing, API codegen) →
  [docs/conventions/REACT.md](docs/conventions/REACT.md)
